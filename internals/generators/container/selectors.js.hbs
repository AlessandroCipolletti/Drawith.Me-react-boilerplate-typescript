import { createSelector } from 'reselect'
import { initialState } from './reducer'

const select{{ properCase name }}Domain = state => state.{{ camelCase name }} || initialState


const makeSelect{{ properCase name }}Key = () =>
  createSelector(
    select{{ properCase name }}Domain,
    {{ camelCase name }}State => {{ camelCase name }}State.key,
  )

export {
  select{{ properCase name }}Domain,
  makeSelect{{ properCase name }}Key,
}
