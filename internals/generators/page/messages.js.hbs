import { defineMessages } from 'react-intl'

export const scope = 'app.pages.{{ properCase name }}'

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the {{ properCase name }} page!',
  },
})
