import { defineMessages } from 'react-intl'

export const scope = 'app.containers.Folder'

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the Folder container!',
  },
})
